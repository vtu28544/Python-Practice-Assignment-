6. Reverse a Number and Find Sum of Digits
n = int(input("Enter a number: "))
temp = n
reverse = 0
digit_sum = 0

while temp > 0:
    digit = temp % 10
    reverse = reverse * 10 + digit
    digit_sum += digit
    temp //= 10

print("Reversed Number:", reverse)
print("Sum of Digits:", digit_sum)
